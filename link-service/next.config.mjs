/** @type {import('next').NextConfig} */
const nextConfig = {
  // Redirect service is intentionally tiny: no images, no React Server Component
  // data fetching beyond the route handlers. Keep defaults.
};

export default nextConfig;
